# Ralph Progress Log — Monthly Calendar App

## Codebase Patterns
- (없음 — 첫 반복에서 US-001 스캐폴딩 후 채워질 예정)

## Pre-Ralph 결정사항 (사람이 확정, 변경 금지)
- 스택: Next.js 16(App Router) + TypeScript + Tailwind + Dexie(IndexedDB) + Vitest
- 저장: 로컬 전용(서버/로그인 없음), Vercel 정적 배포
- 디자인: 스위스 모노크롬 + 하이라인. 색은 할일 막대에만. 시각 레퍼런스 = docs/design/mockups/calendar-layouts.html F 탭
- 캘린더: 무한 세로 스크롤(월 연속, sticky 월 헤더, 오늘로 자동 스크롤). 선 없는 미니멀 그리드
- 색상: 프로젝트=같은 톤 8색조(S58 L46), 태스크종류=톤(dark/tint). applyTone. 32조합 WCAG AA↑. 정본 docs/design/color-system.md
- 마커: 이벤트(◆ 외곽선)/하드 데드라인(⚑ 검은 칩) — 흑백, 막대와 별개
- 타이포: 영문 Plain400(상용) / 숫자·한글 IBM Plex Sans KR(무료), 자간 -3%. docs/design/typography.md
- 할일: 날짜 단위(시간 없음), 여러 날 기간 막대. 태스크종류 전역 공유

## 미해결(Open Questions, 구현 중 기본값으로 진행 가능)
- '+N개' 요약 임계값 (기본: 행에 3~4개 초과 시)
- 프로젝트/태스크종류/마커 삭제 시 소속 항목 처리 (기본: 기본으로 이동)
- 무한 스크롤 가상화 시점 (성능 필요 시)
- 영문 Plain woff2 호스팅(숫자·한글 IBM Plex는 무료 CDN)
- 할일 완료 체크: v1 비범위

---

## 2026-05-21T21:05:00 - US-001
- Next.js 16 (App Router) + React 19 + TypeScript + Tailwind v4 스캐폴딩 (create-next-app, --no-src-dir, alias `@/*`).
- Dexie 4 설치 + `lib/db/index.ts`에 빈 `CalendarDB` 인스턴스(version 1, stores 비어있음 — 스키마는 US-002).
- Vitest 4 + jsdom + @testing-library/{react,jest-dom,dom} 설정 (`vitest.config.ts`, `vitest.setup.ts`). 스모크 테스트 `lib/db/db.test.ts` 통과.
- 디자인 기반: `app/globals.css`를 스위스 모노크롬 셸(흑백 토큰)+스크립트 분리 폰트(IBM Plex Sans KR 무료 로드 + Plain @font-face 폴백), 자간 -3%로 교체. 다크모드 제거(라이트 전용).
- `app/layout.tsx` lang="ko", Geist 폰트 제거. `app/page.tsx` 데모 제거 → 빈 홈 셸. 데모 SVG 삭제.
- package.json scripts: typecheck/test/test:watch 추가. .gitignore: next-env.d.ts, *.tsbuildinfo 추가. eslint: docs/** 무시.
- 파일: package.json, package-lock.json, tsconfig.json, next.config.ts, postcss.config.mjs, eslint.config.mjs, README.md, app/{globals.css,layout.tsx,page.tsx}, lib/db/{index.ts,db.test.ts}, vitest.config.ts, vitest.setup.ts, .gitignore
- 품질: typecheck ✓ / lint ✓(0 errors) / test ✓(1 passed). 헤드리스 dev 부팅 + curl로 `GET / 200` 확인(콘텐츠·lang="ko" 렌더, 서버 에러 없음).
- Needs human browser verification: 빈 홈 화면의 시각적 렌더(폰트 적용/모노크롬 표시) — 코드 경로는 확인됨(HTTP 200).
- Learnings for future iterations:
  - Tailwind v4: `@import "tailwindcss"`가 규칙으로 확장되므로 원격 `@import url(...)`(Google Fonts)는 반드시 그 **위**에 둬야 함. 아니면 dev에서 CSS 파싱 500 발생.
  - 스택 실측: next 16.2.6, react 19.2.4, dexie 4.4.2, vitest 4.1.7, tailwind v4. create-next-app은 Tailwind v4(@tailwindcss/postcss, tailwind.config 없음, CSS @theme 토큰)로 셋업.
  - 루트에 기존 파일(docs/scripts/tasks)이 있어 create-next-app은 /tmp에서 생성 후 머지함. 루트 CLAUDE.md/AGENTS.md는 머지 제외(Ralph 지침 보존).
  - Dexie 인스턴스 생성은 SSR-safe(생성자는 IndexedDB 미접근, open() 시 접근). 클라이언트 컴포넌트에서 import할 것.
  - 포트 3000 사용 중이면 Next dev가 자동으로 3003 등으로 바인딩 → dev 로그에서 실제 포트 확인 필요.
  - 단위 테스트가 IndexedDB를 열려면(US-002 CRUD) `fake-indexeddb` 추가 필요(현재 미설치).
---

## 2026-05-21T21:12:00 - US-002
- 데이터 모델: `lib/types.ts`에 Project/TaskType/Task/Marker + DateString/Timestamp/ToneMode/MarkerKind 정의. 날짜=YYYY-MM-DD 문자열, createdAt/updatedAt=epoch ms.
- Dexie 스키마: `lib/db/index.ts` v1 stores — id(문자열 UUID) PK + 인덱스(projects: order,visible / taskTypes: order / tasks: projectId,taskTypeId,startDate,endDate / markers: date,kind,projectId). 테이블 타입을 클래스 필드로 선언.
- CRUD: 엔티티별 파일(projects/taskTypes/tasks/markers.ts) — create(Input 타입, id/createdAt 생성 후 레코드 반환)/get/getAll(정렬: project·taskType=order, task=startDate, marker=date)/update/delete. Task는 createTask가 createdAt=updatedAt, updateTask가 updatedAt 자동 갱신. getTasksByProject 추가.
- 색 시드 상수: `lib/color/tokens.ts`에 PROJECT_COLORS(8색조)·TASK_TYPE_TONES(4톤)·DEFAULT_PROJECT_COLOR(#3175B9 블루)·DEFAULT_PROJECT_NAME("기본"). applyTone/barText(로직)은 US-006로 분리.
- 시드: `lib/db/seed.ts` seedIfEmpty() — projects/taskTypes 둘 다 비었을 때만 트랜잭션으로 기본 프로젝트 1 + 태스크종류 4 시드. 멱등.
- 앱 연결: `components/db-init.tsx`(use client) useEffect로 seedIfEmpty 1회 실행, `app/layout.tsx` body에 마운트.
- 테스트: `lib/db/crud.test.ts` 8개(프로젝트/태스크종류/태스크/마커 CRUD + 정렬·필터 + 시드·멱등). `vitest.setup.ts`에 `fake-indexeddb/auto` 추가, beforeEach 전 테이블 clear.
- 파일: lib/types.ts, lib/color/tokens.ts, lib/db/{index.ts,util.ts,projects.ts,taskTypes.ts,tasks.ts,markers.ts,seed.ts,crud.test.ts}, components/db-init.tsx, app/layout.tsx, vitest.setup.ts, package.json(+fake-indexeddb dev)
- 품질: typecheck ✓ / lint ✓(0 errors) / test ✓(9 passed: scaffold 1 + crud 8).
- Needs human browser verification: none (US-002 AC에 "Verify in browser" 항목 없음). 단, 새로고침 후 IndexedDB 유지는 실제 브라우저에서만 확인 가능 — DbInit 마운트 + Dexie open 경로는 구현됨.
- Learnings for future iterations:
  - ID=문자열 UUID(crypto.randomUUID, util.ts에 폴백 포함). Dexie stores에서 `++` 미사용 → add 시 우리가 id 부여.
  - 시드는 DbInit(클라이언트)에서 마운트 시 실행. 다른 클라이언트 컴포넌트도 `@/lib/db`에서 db·CRUD·seedIfEmpty 모두 import 가능(index.ts 배럴 재export, db가 재export보다 먼저 정의돼 순환 안전).
  - 테스트 격리: 싱글톤 db 유지 + beforeEach에서 4개 테이블 clear. `fake-indexeddb/auto`는 vitest.setup.ts에서 전역 로드(jsdom엔 IndexedDB 없음).
  - Task 시간 정밀도: createdAt===updatedAt 가능(같은 ms). 비교는 >= 사용.
  - 다음(US-003) 캘린더 렌더는 getAllTasks(startDate 정렬)·getAllProjects(order)·getAllTaskTypes(order)·getAllMarkers(date) 사용. applyTone/barText는 US-006에서 lib/color/compose.ts로 추가 예정.
---

## 2026-05-21T21:18:00 - US-003
- 무한 스크롤 먼슬리 캘린더(선 없는 그리드). 막대/마커는 아직 없음(US-005/US-017) — 이번엔 그리드 셸만.
- 순수 로직 분리:
  - `lib/calendar/dates.ts`: UTC-noon 앵커 날짜 유틸 — parseDate/toDateString/addDays/diffDays/todayDateString. 노이즈(DST) 회피 위해 정오 고정.
  - `lib/calendar/infinite.ts`: buildWeeksRange(from,to,today) 그리드정렬 주 생성(일~토) + groupWeeksByMonth(목요일=week[4] 규칙) + addMonths(연도 래핑) + monthLabel + WEEKDAYS.
- `components/calendar/CalendarView.tsx`(use client):
  - 초기 범위 today±3개월, 양 끝 EXPAND_PX(600) 근접 시 월 1개씩 확장. prepend 시 prependAnchor(이전 scrollHeight)로 useLayoutEffect에서 scrollTop 보정 → 위로 늘려도 점프 없음.
  - 마운트 시 [data-today]를 뷰포트 중앙으로 자동 스크롤(centerToday). '오늘' 버튼=smooth로 동일 동작.
  - 스크롤 시 sticky 헤더에 걸린 월(top-relative ≤8px의 마지막 section)로 상단 타이틀(YYYY/MM) 갱신.
- `app/globals.css`: 에디토리얼(F탭) 그리드 CSS 포팅 — .app-shell/.ed-main/.ed-bar/.ed-ttl/.ed-today/.ed-calwrap/.month-head(sticky)/.month-name/.cal-head/.cal-hd/.cal-week(min-h 88, 선 없음)/.cal-cell(.out=opacity .4)/.cal-daynum/today=검은 원.
- `app/page.tsx`: 빈 셸 → <main.app-shell><CalendarView/>. 우측 할일 패널·프로젝트 탭은 후속 스토리.
- 파일: lib/calendar/{dates.ts,dates.test.ts,infinite.ts,infinite.test.ts}, components/calendar/CalendarView.tsx, app/{globals.css,page.tsx}
- 품질: typecheck ✓ / lint ✓(0) / test ✓(26 passed: 기존 9 + 신규 17 캘린더 로직). dev 부팅 GET / 200, SSR HTML에 month-name 8·cal-daynum 217·data-today 1·ed-ttl 1 렌더, 서버 에러/경고 없음.
- Needs human browser verification:
  - 월 경계가 끊김 없이 세로로 이어지는지(시각)
  - 로드 시 오늘(2026-05-21)로 자동 스크롤 + '오늘' 버튼 복귀
  - 스크롤 시 상단 타이틀이 현재 월로 갱신
  - 위/아래 스크롤 시 인접 월이 점프 없이 추가(특히 위로 prepend 시 앵커링)
  - sticky 월 헤더 고정, 오늘=검은 원/타 월 흐림 등 미니멀 그리드 룩
- Learnings for future iterations:
  - 월 그룹=목요일 규칙이라 그리드정렬 특성상 범위 양 끝에 "1주짜리 인접 월 그룹"이 생길 수 있음(정상). today±3 버퍼로 중앙은 항상 온전한 월.
  - 무한 확장 안정화: onScroll은 didInitialScroll 후에만 동작(초기 프로그램 스크롤이 확장 오발동 안 하게). prepend는 layout effect에서 scrollHeight 델타로 보정.
  - useLayoutEffect 2개(초기 스크롤/ prepend 앵커)는 선언 순서대로 실행 — 초기 스크롤이 먼저, ref 가드로 1회만.
  - US-005 막대: .cal-week에 lane 수 기반 inline height 부여 예정(현재 min-height 88 placeholder). left%/width%는 7열(/7*100) 기준. layoutWeek(인터벌 스케줄링)은 lib/calendar/layout.ts로 분리 + 테스트(US-007).
  - 날짜 비교/연산은 전부 dates.ts(UTC-noon)로 — 컴포넌트에서 new Date 직접 계산 금지(tz drift).
  - CalendarView는 'use client' + SSR도 됨. useLayoutEffect SSR 경고 미발생 확인(GET 200, 로그 clean). today는 서버/클라 동일일자 가정(로컬 앱).
---

## 2026-05-21T21:25:00 - US-004
- 일칸 드래그로 기간 선택 → 등록 팝오버 오픈. 저장/폼은 US-005 범위(이번엔 선택+팝오버 셸까지).
- 순수 로직 분리: `lib/calendar/selection.ts`
  - normalizeRange(a,b): 역방향 드래그 정규화(start<=end). isWithinRange(date,start,end): 양끝 포함. formatRangeLabel: 단일 "5/21" / 다일 "5/21 ~ 5/24"(앞자리 0 제거, parseDate UTC-noon 비교).
- `components/calendar/CreatePopover.tsx`(use client): 선택 기간 헤더 표시 + ✕/ESC/투명 backdrop 바깥클릭 닫힘. 그림자 없음(스위스=하이라인만), release 좌표에 앵커 후 useLayoutEffect로 뷰포트 클램프, subtle cpIn(120ms fade/rise). role=dialog. 카드 내부 pointerdown stopPropagation으로 backdrop 닫힘 방지.
- `CalendarView.tsx`: pointer 기반 드래그 선택.
  - onPointerDown(기본버튼만, e.preventDefault로 네이티브 텍스트선택 차단) → anchor/current = 그 날짜, 기존 selection 닫음. onPointerEnter → current 갱신(드래그 중일 때만). window pointerup → normalizeRange 확정 후 selection+popoverPos(e.clientX/Y) 세팅. pointercancel → 리셋(팝오버 안 염).
  - draggingRef/anchorRef/currentRef = window 핸들러 진실원본(state stale 회피), isDragging/dragAnchor/dragCurrent state = 라이브 하이라이트 렌더. highlight=드래그 중이면 (anchor,current) 정규화, 아니면 committed selection(팝오버 열린 동안 셀 점등 유지).
  - 단일 칸 클릭 = 하루(anchor=current → start=end). 셀에 .sel 클래스(모노크롬 var(--sel) fill, 인접 셀이 한 띠로 읽힘), .ed-calwrap.is-selecting{user-select:none}, .cal-cell{cursor:cell}.
- 파일: lib/calendar/{selection.ts,selection.test.ts}, components/calendar/{CreatePopover.tsx,CalendarView.tsx}, app/globals.css
- 품질: typecheck ✓ / lint ✓(0) / test ✓(38 passed: 기존 26 + 신규 12 selection). dev 부팅 GET / 200, SSR 217셀·data-today 1 렌더, 서버 에러/경고 없음(popover는 클라 전용 — 인터랙션 후 마운트).
- Needs human browser verification:
  - 드래그 시 지나간 칸들이 라이브로 하이라이트되고, 드래그 범위와 팝오버 표시 기간이 일치하는지
  - 드래그 종료 시 팝오버가 release 위치 근처에 뜨고 뷰포트 밖으로 안 넘치는지
  - 단일 클릭 시 하루짜리 팝오버
  - 역방향 드래그(종료→시작)도 동일 범위로 정규화
  - ESC/바깥 클릭으로 닫힘, 닫히면 하이라이트 해제
- Learnings for future iterations:
  - 드래그 멀티셀 선택은 setPointerCapture를 쓰면 안 됨 — 캡처하면 다른 셀의 pointerenter가 안 옴. 캡처 없이 cell별 onPointerEnter + window pointerup(바깥 release 캡처)로 구현. 마우스 우선(데스크톱) 전제.
  - window 이벤트 핸들러 안에서는 state가 stale → anchorRef/currentRef/draggingRef로 읽고, 렌더용 state는 별도로 미러링.
  - 팝오버는 그림자 금지(디자인 규칙) → 하이라인 카드 + 흰 배경 + subtle 페이드로 떠 보이게.
  - US-005는 CreatePopover를 확장: 제목 input(빈 제목 차단) + 프로젝트/태스크종류 select(현재 뷰 프로젝트 기본값) + 저장(createTask) → 팝오버 닫고 막대 렌더. selection 범위(start/end)를 그대로 createTask에 전달. 막대 분할/색은 US-005~007.
  - 막대(US-005)와 선택 하이라이트가 같은 셀에 공존: 하이라이트는 셀 배경, 막대는 .cal-week 위 absolute lane이므로 충돌 없음.
---

## 2026-05-21T21:34:00 - US-005
- 드래그 선택 → 등록 폼 → createTask 저장 → 캘린더 막대 렌더. 주 경계 분할 포함. 색 톤(US-006)/레인 패킹(US-007)은 후속.
- 순수 로직 분리: `lib/calendar/segments.ts`
  - weekSegments(week, tasks): 각 task를 그 주(week[0].ts~week[6].ts)에 클립 → {startCol(0~6), endCol(0~6), contL(이전주에서 연속), contR(다음주로 연속)}. 비교차 task 제외. parseDate/diffDays(UTC-noon)로 tz drift 없음. start 오름차순 + 동률 시 긴 막대 우선 정렬(naive lane 안정).
- `components/calendar/CreatePopover.tsx`(US-004 셸 → 폼 확장):
  - 제목 input(autofocus) + trim 빈값 저장 차단(인라인 "제목을 입력하세요", aria-invalid). 프로젝트 select(defaultProjectId=현재 뷰 프로젝트, fallback projects[0]) + 종류 select(taskTypes[0]). 저장 버튼(onSubmit/Enter, projects/taskTypes 없으면 disabled). onCreate{title,projectId,taskTypeId} 콜백 — 날짜 범위·DB는 부모가 처리. ESC/backdrop/✕ 닫힘 유지. 하이라인 input(밑줄)·select(테두리), 그림자 없음.
- `components/calendar/CalendarView.tsx`:
  - 마운트 시 seedIfEmpty()(DbInit와의 레이스 회피) 후 getAllProjects/getAllTaskTypes/getAllTasks 로드 → state. projectsById 맵.
  - 주별 weekSegments 렌더: lane=세그먼트 배열 인덱스(naive — 겹침 비은폐, 비겹침도 각자 레인=공간 낭비, US-007이 패킹+레인 재사용+'+N개'로 대체). 막대 left=(startCol/7)*100%, width=calc(((endCol-startCol+1)/7*100)% - 4px), top=HEAD_H+lane*(BAR_H+BAR_GAP). 행 minHeight=max(ROW_MIN 88, HEAD_H 30 + segs*(BAR_H 20+BAR_GAP 3)+6). contL/contR 모서리 사각(연속처럼 보임).
  - 저장: handleCreate→createTask(selection.start/end + draft)→tasks state append→팝오버 닫힘(즉시 막대). 새로고침 유지=Dexie 영속+마운트 로드.
  - 막대 bg=project.color(원색 임시, applyTone+barText는 US-006), color=#fff. .cal-bar pointer-events:none(아래 셀 드래그선택 비차단; 막대 클릭/드래그=US-009/010). barIn(140ms fade/rise) 등장.
- 파일: lib/calendar/{segments.ts,segments.test.ts}, components/calendar/{CreatePopover.tsx,CalendarView.tsx}, app/globals.css
- 품질: typecheck ✓ / lint ✓(0) / test ✓(46 passed: 기존 38 + 신규 8 segments). dev 부팅 GET / 200, SSR 217셀·data-today 1·month-name 8 렌더, 서버 에러/경고 없음(막대는 클라 전용 — DB 로드 후 마운트되어 SSR HTML엔 없음 = 정상).
- Needs human browser verification:
  - 드래그→제목 입력→저장 시 선택 기간을 덮는 막대가 즉시 뜨는지
  - 주 경계를 넘는 기간(예 토→다음주)이 주별 연속 막대로 분할되고 모서리가 이어져 보이는지
  - 막대에 제목 텍스트가 보이고(길면 …말줄임), 새로고침 후에도 유지되는지
  - 빈 제목 저장 차단 메시지
- Learnings for future iterations:
  - 막대 bg는 US-005에서 project.color 임시(현재 도달 가능한 상태=기본 블루 프로젝트뿐이라 흰 텍스트 대비 양호). US-006에서 lib/color/compose.ts(applyTone(project.color,{mode,k}) + barText(bg)→흑/백)로 교체하고 CalendarView 막대 style의 background/color를 거기서 계산. taskType은 이미 task에 저장됨 → 톤만 입히면 됨.
  - lane=인덱스는 naive. US-007: lib/calendar/layout.ts에 인터벌 스케줄링(laneEnds[lane]>=startCol면 다음 레인, 아니면 재사용) + '+N개' 임계(행 3~4 초과)로 교체 + 단위 테스트. 막대 렌더 루프는 segs→layout 결과(lane 포함)로 바꾸면 됨.
  - flex 컨테이너에선 bare text-node가 ellipsis 안 됨 → 제목을 .cal-bar-label span으로 감싸 overflow/ellipsis 적용.
  - 막대는 'use client' 마운트 후 DB 로드 결과라 SSR/curl HTML엔 안 보임(정상). 시각 검증은 브라우저 필요. liveQuery(dexie-react-hooks) 미도입 — 현재 단일 컴포넌트라 state append로 충분. US-008 사이드패널이 같은 tasks를 공유해야 하면 그때 상위로 끌어올리거나 useLiveQuery 도입 검토.
  - HEAD_H/BAR_H/BAR_GAP는 CalendarView 상수 + .cal-bar height/line은 inline. CSS .cal-bar는 위치 무관 스타일(폰트/색 외)만.
---

## 2026-05-21T21:39:00 - US-006
- 막대 색상 계산: 프로젝트 원색 + 태스크종류 톤(dark/tint) 합성 + WCAG 대비 자동 텍스트색. US-005의 임시 project.color 직접 사용을 톤 합성으로 교체.
- 순수 로직 분리: `lib/color/compose.ts`
  - applyTone(hex,{mode,k}): dark=c×(1-k), tint=c×(1-k)+255×k. 채널별 Math.round + [0,255] clamp.
  - barText(bgHex): WCAG relative luminance(채널 sRGB 선형화 → 0.2126/0.7152/0.0722 가중) 기반 대비비 계산 → 흰(#FFFFFF)/근검정(#1A1A1A) 중 대비 높은 쪽. 마감=흰, 작업/회의/리서치=검(전 8색조).
  - hexToRgb/rgbToHex(#붙임/없음 모두 파싱, 대문자 출력) + barColors(projectColor,taskType)={background,text} 편의 함수.
- `components/calendar/CalendarView.tsx`: taskTypesById useMemo 맵 추가, 막대 렌더에서 project+taskType 있으면 barColors(project.color, taskType)로 background/color 결정(없으면 var(--text)/흰 폴백). seg.task.taskTypeId로 톤 조회.
- 검증: docs/design/color-system.md §4의 8색조×4톤=32조합 매트릭스를 8×4=32 케이스로 1:1 비교(전부 일치 — 수기로도 채널별 재확인). barText는 매트릭스 괄호 규칙(마감 흰/나머지 검)과 순흑·순백 케이스 검증. AC3/4(동일 프로젝트 톤 구분, 타 프로젝트 색조 구분)는 Set 크기로 검증.
- 파일: lib/color/{compose.ts,compose.test.ts}, components/calendar/CalendarView.tsx
- 품질: typecheck ✓ / lint ✓(0) / test ✓(94 passed: 기존 46 + 신규 48 compose). dev 부팅 GET / 200, SSR 217셀·month-name 8·data-today 1 렌더, 서버 에러/경고 없음(막대는 클라 전용 — DB 로드 후 마운트라 SSR HTML엔 없음 = 정상).
- Needs human browser verification:
  - color-system.md §4 매트릭스처럼 같은 프로젝트의 4종류가 톤(진하기)으로, 다른 프로젝트가 색조로 구분되는지(실제 막대 색)
  - 마감 막대 흰 글자 / 나머지 검은 글자 가독성(대비)
  - (현재 시드는 기본 블루 프로젝트 1개 — 다중 프로젝트는 US-011에서 추가 후 색조 구분 시각 확인 가능)
- Learnings for future iterations:
  - applyTone은 채널별 Math.round일 때만 §4 매트릭스와 정확히 일치(예: 36.6→37=0x25). truncate/floor 쓰면 어긋남. clamp는 임의 hex 안전용(시드 8색은 범위 내).
  - barText는 매트릭스를 하드코딩하지 않고 WCAG 대비비로 계산 → 32조합 전부 매트릭스 괄호 규칙과 자동 일치(마감만 흰). 향후 사용자 추가 색/톤도 자동 대응.
  - barText 검은색은 #1A1A1A(compose.ts 시그니처) — UI 모노크롬 텍스트 #141414와 다름(막대 가독성 전용). 혼동 주의.
  - US-007(레인 패킹)은 막대 색 로직 불변 — barColors 그대로 사용, lane 계산만 lib/calendar/layout.ts로 교체. US-009 편집 시 프로젝트/종류 변경하면 barColors가 자동 재계산(렌더 시 호출)이라 색 즉시 반영됨.
  - 막대 색은 barColors 호출이 렌더마다 일어남(가벼운 산술이라 메모 불필요). 막대 수 급증 시 useMemo로 task→colors 캐시 고려.
---

## 2026-05-21T21:45:00 - US-007
- 겹치는 막대 레인 스택 레이아웃 + '+N개' 오버플로 요약/펼침. US-005의 naive lane=인덱스를 그리디 인터벌 스케줄링으로 교체.
- 순수 로직 분리: `lib/calendar/layout.ts`
  - layoutWeek(segments, maxLanes=DEFAULT_MAX_LANES 3): 그리디 인터벌 스케줄링. laneEnd[lane]=그 레인의 마지막 점유 열, seg.startCol이 그보다 크면(겹침 없음) 그 레인 재사용, 아니면 새 레인. findIndex로 "첫 빈 레인" 선택 → 인터벌 그래프 최소 레인 수(=최대 동시 겹침)와 일치. 겹침 판정=inclusive [startCol,endCol] 교차(한 열만 닿아도 겹침).
  - 반환 WeekLayout{ segments: PlacedSegment[](각 seg + lane), laneCount, overflow: OverflowChip[] }. overflow=lane>=maxLanes 막대가 덮는 열별 카운트({col,count}) — collapsed일 때 '+N개' 칩 위치/개수.
- `components/calendar/CalendarView.tsx`:
  - weekSegments → layoutWeek(segs, 3)로 교체. expandedWeeks: Set<string>(주의 일요일 date 키) state + toggleWeek로 펼침/접힘.
  - collapsed: lane<3 막대만 렌더 + overflow 열마다 '+N개' 칩(lane=3 행)을 그려 클릭 시 그 주 펼침. expanded: 전체 막대 + 좌측 '접기' 칩(lane=laneCount 행)으로 다시 접음. 막대 top=HEAD_H+seg.lane*(BAR_H+BAR_GAP). 행 minHeight=visibleLanes(+오버플로 시 칩 1행).
  - .cal-more: 모노크롬 하이라인 칩(색은 막대에만 규칙 유지), barIn 등장, hover=var(--sel). 버튼이라 pointer-events 기본 auto(막대는 none) → 클릭 동작.
- 파일: lib/calendar/{layout.ts,layout.test.ts}, components/calendar/CalendarView.tsx, app/globals.css
- 품질: typecheck ✓ / lint ✓(0) / test ✓(105 passed: 기존 94 + 신규 11 layout). dev 부팅 GET / 200, SSR 217셀·data-today 1·month-name 8 렌더, 서버 에러/경고 없음(막대·칩은 클라 전용 — DB 로드 후 마운트라 SSR HTML엔 없음 = 정상).
- Needs human browser verification:
  - 같은 주에 기간이 겹치는 막대 3개가 서로 다른 레인에 모두 보이는지(가려짐 없음)
  - 겹치지 않는 막대가 같은 레인을 재사용해 공간을 아끼는지
  - 4개 이상 겹칠 때 '+N개' 칩이 뜨고, 클릭하면 펼쳐져 전부 보이고 '접기'로 다시 접히는지
  - '+N개' 칩이 인접 열에서 겹쳐 보이지 않는지(열 폭이 좁아 칩이 붙는 드문 케이스)
- Learnings for future iterations:
  - 그리디 "첫 빈 레인" 배치는 weekSegments의 정렬(startCol↑, 긴 막대 우선)에 의존해 안정적. 정렬이 바뀌면 lane 배치(시각)도 바뀌니 segments.ts 정렬 변경 시 주의.
  - 겹침=inclusive 교차 → 한 칸만 맞닿아도(예: A끝=B시작 같은 열) 다른 레인. 막대가 같은 칸을 시각적으로 공유하면 안 되므로 의도된 동작.
  - overflow 칩은 열 단위(AC "한 칸에 막대가 너무 많으면"). 인접 열 둘 다 오버플로면 칩이 시각적으로 겹칠 수 있음(열 폭 ~14%, 칩 ~30px) — 드물고(4+ 막대 인접) 펼치면 해소. 필요 시 주당 칩 1개 요약으로 단순화 가능.
  - 펼침 상태는 expandedWeeks(메모리)만 — 새로고침 시 초기화(접힘). 영속 불필요(뷰 상태). 주 키=week[0].date(일요일)라 무한 스크롤로 같은 주가 재마운트돼도 상태 유지.
  - DEFAULT_MAX_LANES=3은 lib/calendar/layout.ts 상수. US-008 사이드패널/US-013 통합 뷰에서 막대 수↑ 시 임계 조정 가능(open question §3 기본 3~4 유지).
  - US-009 편집/삭제·US-010 이동으로 tasks 변하면 layoutWeek가 렌더마다 재계산(주당 가벼움). 막대 클릭(US-009)/드래그(US-010)는 .cal-bar pointer-events:none을 그 스토리에서 풀어야 함 — '+N개' 칩은 이미 auto라 충돌 없음.
---

## 2026-05-21T21:55:00 - US-008
- 오른쪽 "할일" 사이드 패널(날짜순 목록) + 패널↔캘린더 연결. 공유 상태를 상위로 끌어올림(US-005 노트 예고대로).
- 구조 리팩터:
  - `components/calendar/CalendarApp.tsx`(use client, 신규): projects/taskTypes/tasks + projectsById/taskTypesById를 소유. 마운트 시 seedIfEmpty+getAll* 로드(CalendarView에서 이관). createTask는 handleCreateTask에서 수행 후 tasks append. fragment로 <CalendarView/>(.ed-main)+<TaskListPanel/>(.ed-list) 렌더 → page.tsx의 .app-shell이 flex 컨테이너라 둘이 형제 flex child.
  - `app/page.tsx`: <CalendarView/> → <CalendarApp/>.
  - `CalendarView.tsx`: 자체 데이터 state/로드/seed 제거, props 기반 presentational로 전환. handleCreate→onCreateTask(NewTaskInput) 위임. projectsById/taskTypesById도 props로 받음.
- `components/calendar/TaskListPanel.tsx`(use client, 신규): tasks를 parseDate(startDate) 오름차순(동률 title.localeCompare) 정렬. 항목=버튼(.tp-item): tp-dot(barColors(project.color,taskType).background = 색조+톤 스와치, 막대색과 동일)+tp-title(말줄임)+tp-meta(formatRangeLabel · 프로젝트명 · 종류명). 헤더 numbadge=tasks.length + "날짜순". 빈 상태 .tp-empty("일칸을 드래그해 첫 할일을 추가하세요"). 푸터 .ed-contact "↳ ＋ 할일 추가"(onAdd).
- 패널→캘린더 강조(AC4): CalendarApp이 highlightedTaskId(.cal-bar.hl 모노크롬 outline 링)+highlightNonce(반복 클릭 재스크롤) 보유, handleSelectTask가 1.8s 후 자동 해제(타이머 ref, 언마운트 정리). CalendarView highlight effect: weekKeysInRange로 막대가 덮는 모든 주를 expandedWeeks에 추가(오버플로로 숨은 막대도 표시) 후 rAF에서 [data-date=task.startDate] 셀을 뷰포트 중앙으로 smooth 스크롤(셀은 항상 렌더되므로 펼침 전에도 스크롤 타깃 존재). 막대에 data-task-id, 셀에 data-date 부여.
- 패널 "＋ 할일 추가"(AC 외 레이아웃 보강): addNonce 증가 → CalendarView가 오늘 날짜(start=end=today)로 popover 오픈(뷰포트 중앙 근처, 클램프).
- 순수 로직: `lib/calendar/infinite.ts` weekKeyOf(date)=그 주 일요일 키(buildWeeksRange의 week[0].date와 동일), weekKeysInRange(start,end)=구간이 덮는 일요일 키 배열. +7 단위 테스트(중간일/일/토, week[0].date 일치, 동주/2주/3주).
- 파일: components/calendar/{CalendarApp.tsx(신규),TaskListPanel.tsx(신규),CalendarView.tsx}, app/{page.tsx,globals.css}, lib/calendar/{infinite.ts,infinite.test.ts}
- 품질: typecheck ✓ / lint ✓(0) / test ✓(112 passed: 기존 105 + 신규 7). dev GET / 200, SSR에 .ed-list/numbadge/tp-empty/ed-contact + cal-cell 217·data-today 1 렌더, 서버 에러/경고 없음(막대·목록 항목은 클라 DB 로드 후 마운트 = SSR엔 빈 패널/빈 상태가 정상).
- Needs human browser verification:
  - 할일 추가 시 우측 패널이 즉시 갱신되고 numbadge 카운트가 오르는지
  - 항목 클릭 시 캘린더가 해당 막대로 스크롤되고 모노크롬 링으로 강조되는지(오버플로로 숨었던 막대면 그 주가 펼쳐져 보이는지)
  - tp-dot 색이 실제 막대색(색조+톤)과 일치하는지, 메타에 기간/프로젝트/종류가 맞게 표시되는지
  - 빈 상태 문구 표시, ＋할일추가 클릭 시 오늘 날짜 popover 오픈
  - 좁은 화면에서 패널(300px 고정)이 캘린더를 과하게 압박하지 않는지(반응형 접힘은 US 미정의 — 현재 항상 표시)
- Learnings for future iterations:
  - set-state-in-effect(react-hooks 신규 룰): 이펙트 본문에서 동기 setState 금지. 형제 컴포넌트 nonce 트리거에 반응하는 이펙트는 requestAnimationFrame 콜백 안에서 setState하면 룰 통과 + 커밋 단계 밖으로 지연(코드베이스의 기존 패턴=중첩 콜백 안 setState와 일관).
  - 상태 소유 이전: CalendarView가 더 이상 데이터를 로드하지 않음 → tasks/projects/taskTypes는 CalendarApp이 진실원본. US-009 편집/삭제·US-010 이동·US-011/012 관리도 CalendarApp에 mutate 핸들러(onUpdateTask/onDeleteTask 등) 추가하고 양쪽에 내려주면 패널·캘린더 동시 갱신. (현재 onCreateTask만 존재)
  - DbInit(컴포넌트)와 CalendarApp 둘 다 seedIfEmpty 호출 — seedIfEmpty가 멱등이라 안전. CalendarApp에서 로드 전에 seed 보장.
  - 강조 링은 막대에만 outline(모노크롬). 패널 tp-dot은 막대색과 동일(barColors)이라 패널↔막대 시각적 동일성 확보 — US-009에서 색 변경 시 양쪽 자동 일치.
  - AC "현재 뷰/필터와 동기화": 아직 뷰 전환(US-013)·표시토글(US-014)·종류필터(US-015) 미구현 → 현재는 캘린더·패널 둘 다 전체 tasks를 필터 없이 렌더하므로 자동 동기화. 그 스토리들에서 필터를 공유 상태(CalendarApp)에 두고 양쪽에 적용하면 동기 유지.
  - 패널 항목은 <button>이라 자식은 phrasing content만 가능(div 금지) → tp-body/title/meta를 <span>+display:block으로 구성.
  - addNonce/highlightNonce 패턴: 초기값 0은 마운트 시 무시(addNonce===0 가드). 클릭마다 +1.
---

## 2026-05-21T22:05:00 - US-009
- 막대 클릭 → 편집/삭제 팝오버. 편집/삭제가 캘린더+사이드 패널 양쪽에 즉시 반영(공유 tasks state 덕). 드래그 이동(4px 임계)은 US-010 범위.
- `components/calendar/EditPopover.tsx`(신규): 막대 클릭 시 기존 값 prefill(title/project/type/start/end). 제목 input(autofocus+select, trim 빈값 차단 '제목을 입력하세요' 인라인). native `<input type=date>` 2개(시작/종료) — 값=YYYY-MM-DD라 DateString과 직결, 별도 파싱 불필요. 저장 시 normalizeRange(start>end면 자동 스왑 — US-004 드래그 규칙과 동일하게 forgiving)로 보정 후 onSave(EditTaskDraft). 라이브 기간 라벨(formatRangeLabel)이 날짜 입력 따라 갱신. 삭제=2단계 인라인 확인(삭제 클릭→'삭제할까요? [삭제][취소]') — native confirm() 대신 인라인(스위스 하이라인/그림자 없음 유지, 테스트·a11y 용이). ESC/✕/투명 backdrop 닫힘, 뷰포트 클램프, cpIn 애니메이션. cp-* 스타일 재사용.
- `components/calendar/CalendarView.tsx`: 막대 div에 role="button"+tabIndex=0+aria-label "{제목} — 편집" + onClick(openEdit, e.clientX/Y 앵커) + onKeyDown(Enter/Space → e.currentTarget.getBoundingClientRect()로 위치, preventDefault). editingTaskId/editPos state, editingTask=useMemo(tasks.find). openEdit는 진행 중 create selection 닫음(상호배타). handleEditSave→onUpdateTask 후 closeEdit, handleEditDelete→onDeleteTask 후 closeEdit. EditPopover를 CreatePopover와 나란히 조건부 렌더. onUpdateTask/onDeleteTask props 추가.
- `components/calendar/CalendarApp.tsx`: handleUpdateTask(updateTask DB + setTasks map으로 changes merge + updatedAt=Date.now())/handleDeleteTask(deleteTask DB + setTasks filter + 삭제 대상이 현재 highlight면 해제). CalendarView에 두 핸들러 전달. handleDeleteTask는 setHighlightedTaskId 선언 이후로 이동(아래 학습 참고).
- `app/globals.css`: .cal-bar pointer-events:none→auto + cursor:pointer + hover(1.5px)/focus-visible(2px) 모노크롬 outline(색은 막대 fill에만 유지). .cp-select에 .cp-date 묶어 동일 하이라인 스타일 + tabular-nums. .cp-foot(삭제 좌/저장 우)/.cp-del/.cp-confirm(-q/-yes/-no) 추가.
- 파일: components/calendar/{EditPopover.tsx(신규),CalendarView.tsx,CalendarApp.tsx}, app/globals.css, scripts/ralph/{prd.json,progress.txt}
- 품질: typecheck ✓ / lint ✓(0) / test ✓(112 passed — 신규 순수 로직 없음: 날짜 정규화는 기존 normalizeRange 재사용, 이미 12 selection 테스트로 커버). dev GET / 200, SSR 217셀·data-today 1·ed-list 렌더, 서버 에러/경고 없음(팝오버는 클라 전용 — 인터랙션 후 마운트).
- Needs human browser verification:
  - 막대 클릭 시 편집 창이 클릭 위치 근처에 뜨고 기존 값(제목/기간/프로젝트/종류)이 채워지는지
  - 제목/시작·종료일/프로젝트/종류 변경 후 저장이 막대에 반영되고, 프로젝트·종류 변경 시 막대 색(색조/톤)이 즉시 바뀌는지
  - 삭제 2단계 확인 후 막대가 사라지고 사이드 패널 목록·카운트가 즉시 갱신되는지
  - 시작일을 종료일보다 뒤로 입력해도 정상 범위로 정규화되는지
  - 키보드(Tab으로 막대 포커스 → Enter/Space)로도 편집 창이 열리는지, ESC/바깥클릭 닫힘
- Learnings for future iterations:
  - React Compiler 린트(react-hooks/preserve-manual-memoization + 'Cannot access variable before it is declared'): useCallback 본문이 **뒤에 선언된** useState 세터를 참조하면 에러. setHighlightedTaskId를 쓰는 handleDeleteTask는 반드시 그 useState 선언 **아래** 배치할 것. (런타임은 클로저라 OK여도 컴파일러 분석이 막음.)
  - 막대 pointer-events:auto로 전환 — 이제 막대 위에서는 드래그-셀렉션(US-004)이 시작 안 됨(막대가 pointerdown 가로챔). 의도된 동작(막대=편집/이동, 빈 칸=새 선택). US-010 드래그 이동은 이 막대 pointer 이벤트 위에 4px 임계로 click(편집) vs drag(이동) 구분을 얹으면 됨 — onPointerDown에서 시작점 기록, 4px 넘으면 이동 모드, 안 넘고 release면 openEdit 호출.
  - 날짜 편집은 native `<input type=date>`로 충분(값=YYYY-MM-DD=DateString). tz 없는 문자열 그대로라 parseDate 불필요. 정규화만 normalizeRange로.
  - 편집/삭제 즉시 반영의 핵심=CalendarApp이 tasks 단일 진실원본. mutate 핸들러가 DB + setTasks 둘 다 갱신하면 캘린더(막대 색=barColors 렌더마다 재계산)와 패널(스와치/메타) 자동 동기화. US-011/012(프로젝트·종류 편집)도 같은 패턴: projects/taskTypes mutate 핸들러 추가 → 막대 색 자동 재계산.
  - 삭제 확인은 인라인 2단계가 native confirm()보다 디자인(그림자 없는 하이라인)·테스트 친화적. 파괴적 확정 버튼만 채운 검정, 나머지는 하이라인.
  - 막대 hover/focus는 모노크롬 outline(색 규칙 위반 없이 affordance 제공). .hl(패널 강조 2px) vs hover(1.5px) vs focus-visible(2px) outline 공존 — z-index로 우선순위(focus 2 > hover 1).
---

## 2026-05-21T22:15:00 - US-017
- 날짜 마커(이벤트/하드 데드라인) — 흑백 칩으로 색상 막대와 구분. Marker 엔티티/CRUD는 US-002에서 이미 존재 → 상태 소유·렌더·생성/편집/삭제 UI만 추가.
- 순수 로직 분리: `lib/calendar/markers.ts` groupMarkersByDate(markers): 날짜별 버킷 Map + 버킷 내 정렬(데드라인 우선 → createdAt 오름차순). +5 단위 테스트(빈/단일/다중 날짜/데드라인 우선/동종 createdAt 정렬).
- `components/calendar/MarkerPopover.tsx`(신규, 생성/편집 겸용): kind 토글(⚑데드라인 / ◆이벤트, 모노크롬 세그먼트 .mk-kind, role=radiogroup/radio + aria-checked) + 라벨 input(autofocus+select, trim 빈값 차단 인라인 '라벨을 입력하세요') + native `<input type=date>`. marker prop=null이면 생성(기본 kind='deadline', date=defaultDate), 있으면 편집(prefill) + 2단계 인라인 삭제 확인(native confirm 미사용 — cp-foot/cp-confirm-* 재사용으로 EditPopover와 일관). ESC/✕/투명 backdrop 닫힘, 뷰포트 클램프, cpIn 애니메이션. cp-* 스타일 공유.
- `CalendarView.tsx`: markers props + markersByDate=groupMarkersByDate(useMemo). 셀 cal-daynum 뒤에 마커 칩 렌더 — `<button className="mk mk-dl|mk-ev">⚑/◆ 라벨` , onPointerDown e.stopPropagation()(셀 드래그선택 비차단) + onClick→openMarkerEdit(marker, e.clientX/Y). markerPopover state{marker,date,x,y}(생성=marker:null/편집 겸용), openMarkerCreate(closeTaskPopovers 후 — 팝오버 상호배타)/openMarkerEdit, closeMarkerPopover, handleMarkerSave(marker 있으면 onUpdateMarker, 없으면 onCreateMarker)/handleMarkerDelete. markerAddNonce effect(rAF→openMarkerCreate(today, 화면 중앙) — addNonce와 동형). MarkerPopover를 Create/Edit 팝오버와 나란히 조건부 렌더(onDelete는 편집 모드만).
- `CalendarApp.tsx`: markers state + getAllMarkers를 마운트 Promise.all 로드에 추가. handleCreateMarker(createMarker DB + setMarkers append)/handleUpdateMarker(updateMarker + map merge)/handleDeleteMarker(deleteMarker + filter) — 캘린더 칩이 공유 state에서 렌더되어 즉시 반영. markerAddNonce/handleAddMarker(패널 트리거). CalendarView·TaskListPanel에 props 전달. projectId는 v1 미설정(open question §6 마커-프로젝트 연결 보류).
- `TaskListPanel.tsx`: 마커 범례 행(.ed-legend-row) — mk-dl '⚑ 데드라인' + mk-ev '◆ 이벤트'(F탭 미러) + ＋마커 버튼(onAddMarker→markerAddNonce). onAddMarker prop 추가.
- `app/globals.css`: .mk/.mk-dl(검은 채움 #141414)/.mk-ev(외곽선) 칩 — F탭 .mk 미러 + button reset(font-family inherit/background none/border none)·button.mk hover/focus-visible 모노크롬 링. .ed-legend-row(flex gap, 첫 칩 margin-left:0), .mk-add(우측 정렬 하이라인 버튼), .mk-kinds/.mk-kind(.on=검은 채움) 토글. 색은 막대에만 규칙 유지 — 마커는 전부 흑백.
- 파일: lib/calendar/{markers.ts,markers.test.ts}(신규), components/calendar/{MarkerPopover.tsx(신규),CalendarApp.tsx,CalendarView.tsx,TaskListPanel.tsx}, app/globals.css, scripts/ralph/{prd.json,progress.txt}
- 품질: typecheck ✓ / lint ✓(0 errors/0 warnings) / test ✓(117 passed: 기존 112 + 신규 5 markers). dev GET / 200, SSR에 ed-legend-row/mk mk-dl/mk mk-ev/데드라인/이벤트/＋마커 + cal-cell 217·ed-list 렌더, 서버 에러/경고 없음(셀 마커 칩·팝오버는 클라 DB 로드 후 마운트 = SSR HTML엔 범례만 보이는 게 정상).
- Needs human browser verification:
  - 패널 ＋마커 → 팝오버에서 종류(⚑/◆)·라벨·날짜 입력 후 저장 시 해당 날짜 칸에 흑백 칩이 뜨는지(데드라인=검은 채움 / 이벤트=외곽선)
  - 마커 칩 클릭 시 편집 창이 기존 값으로 열리고, 종류/라벨/날짜 변경이 즉시 반영되는지
  - 삭제 2단계 확인 후 칩이 사라지는지, 새로고침 후 마커가 유지되는지(IndexedDB)
  - 마커 칩이 색상 막대와 시각적으로 구분되는지(흑백 vs 색), 칩이 막대 레인과 겹치지 않는지(같은 날 다중 마커가 줄바꿈해 레인 침범하는 드문 케이스)
  - 패널 마커 범례(⚑ 데드라인 / ◆ 이벤트) 표시
- Learnings for future iterations:
  - 마커 생성 진입점=패널 ＋마커(markerAddNonce, addNonce 패턴 재사용). 캘린더 셀 드래그/클릭은 할일(Task) 생성 전용이라 마커와 충돌 회피 — 셀에서 직접 마커 추가는 비도입(필요 시 셀 단일클릭에 '할일/마커' 선택 분기 추가 가능, 단 CreatePopover 흐름 복잡화 주의).
  - 팝오버 상호배타는 cp-backdrop(position:fixed inset:0 z-index:50)가 자연히 보장 — 어느 팝오버든 열리면 backdrop이 캘린더/패널 클릭을 가로채 다른 진입을 차단. openMarkerCreate의 closeTaskPopovers()는 방어적(현재 도달 불가 경로). 따라서 openEdit/onCellPointerDown에서 setMarkerPopover(null) 호출은 불필요(+React Compiler 'access before declared' 회피 — setMarkerPopover가 그보다 늦게 선언됨).
  - 셀 마커 칩은 cal-daynum과 같은 줄(inline-block + vertical-align middle, top ~12~28px). 막대는 top:HEAD_H(30)부터라 단일 칩은 막대와 안 겹침. 같은 날 마커가 많아 줄바꿈하면 레인 영역 침범 가능 — 흔치 않고 시각 폴리시는 Phase C. 필요 시 칩 max 표시 수 + '+N' 요약 도입.
  - 마커 칩 클릭이 드래그선택을 시작하지 않게 onPointerDown에서 stopPropagation → 셀 onPointerDown 미발화 → draggingRef false 유지 → window pointerup 무동작. onClick은 정상 발화(openMarkerEdit).
  - MarkerInput/MarkerChanges는 lib/db/markers.ts에서 export, 배럴(lib/db) 재export로 import 가능. createMarker는 projectId 옵셔널이라 {kind,label,date}만으로 생성 OK.
  - US-014(범례+토글)가 마커 범례를 사이드바로 옮기거나 통합할 수 있음 — 현재 TaskListPanel 상단 .ed-legend-row에 둠(F탭 위치). US-014에서 프로젝트 범례 행을 그 위에 추가하면 자연스러움.
---
